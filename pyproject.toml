[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "decomb"
version = "0.2.0"
description = "Audited removal of statistically supported sinusoidal components from EEG"
readme = "README.md"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
requires-python = ">=3.11"
authors = [{ name = "Joshua Duquette" }]
keywords = ["eeg", "bids", "mne", "line noise", "harmonics", "sinusoid removal"]
dependencies = [
    "matplotlib>=3.8",
    "mne>=1.6",
    "mne-bids>=0.14",
    "numpy>=1.24",
    "pandas>=2.0",
    "pybv>=0.7.5",
    "pyyaml>=6.0",
    "scipy>=1.11",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6"]

[project.scripts]
decomb = "decomb.cli:main"

[project.urls]
Homepage = "https://github.com/JoshuaDuq/decomb"
Issues = "https://github.com/JoshuaDuq/decomb/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
decomb = ["defaults.yaml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
