# Python build and packaging
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
*.whl
*.tar.gz

# Environments
.venv/
venv/
env/
ENV/
.python-version

# Tool caches and coverage
.pytest_cache/
.ruff_cache/
.mypy_cache/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Editors, notebooks, and OS metadata
.DS_Store
Thumbs.db
.idea/
.vscode/
.ipynb_checkpoints/
*.swp
*~
*.orig
*.rej

# Site configuration and secrets. decomb.yaml holds absolute paths to one site's
# dataset, so it is deliberately not shared.
decomb.yaml
.env
.env.*

# Pipeline outputs and run artifacts. Everything here is regenerated by the CLI.
outputs/
data/
logs/
trials/
*.log

# Generated PSD stage products. `decomb psd` writes these; they are not documentation
# sources. Inert until the copies already tracked under docs/ are removed from the index.
psd_before_after.png
psd_before_after_panels.png
psd_before_after_per_recording.png
psd_before_after.npz

# Session, planning, and agent scratch artifacts, not part of the package
docs/superpowers/
.claude/settings.local.json
scratch/

# Local experiment and pipeline helpers
/build_cohort.sh
/build_trial*.sh
/make_trial_config.py
/preflight.py
/probe_*.py
/run_pipeline.sh
/run_trials.sh
